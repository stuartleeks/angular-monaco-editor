{
    "name": "angular-monacoeditor",
    "private": true,
    "version": "0.0.0",
    "devDependencies": {
        "bower": "^1.7.7",
        "http-server": "^0.9.0"
    },
    "scripts": {
        "postinstall": "bower install",
        "prestart": "npm install",
        "start": "http-server ./app -a localhost -p 8001 -c-1"
    },
    "dependencies": {
        "monaco-editor": "^0.11.1"
    }
}
